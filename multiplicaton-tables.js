for (tafel = 1; tafel <= 10; tafel++) {
    for (nr = 1; nr <= 10; nr++) {
        uitkomstTafel = nr * tafel
        console.log(nr + " x " + tafel + " = " + uitkomstTafel);
    }
    console.log("Dit was de tafel van " + tafel);
}